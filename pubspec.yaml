name: calculator
description: A new calculator with neumorphic design

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_neumorphic: ^3.0.1
  auto_size_text: ^2.1.0
  expressions: ^0.1.5
  shared_preferences: ^0.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
